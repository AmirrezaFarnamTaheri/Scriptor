# `scriptor-system-bridge`

OS metadata, Scriptor data-directory helpers, and release manifest utilities.

## Shipped

- Platform/system info for diagnostics.
- App data path resolution.
- Release manifest read/verify (`scripts/release/verify-manifest.ps1`).

Used by desktop shell, CLI `system-info`, and packaging scripts.
