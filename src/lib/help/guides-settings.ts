import type { HelpGuide } from './types.ts'

export const settingsGuides: readonly HelpGuide[] = [
  {
    id: 'settings', title: 'Settings and preference scope', category: 'Workspace', policy: 'manual', entry: 'Settings in the top bar.', prerequisite: 'Some settings require an open vault or the native desktop app.',
    safety: 'Application preferences, vault configuration, and external-account setup have different persistence and permission boundaries.', source: 'src/components/SettingsPanel.tsx', roots: ['.settings-panel'],
    steps: [['Choose a category', 'Use General, Appearance, Editor, Integrations, Shortcuts, or Advanced according to the job rather than searching one long form.'], ['Check scope', 'Distinguish settings belonging to this vault from local UI preferences. Changing vault configuration can affect other users of the same folder.'], ['Save deliberately', 'Observe the surface persistence note and Save/Apply action. Do not assume every field follows the same immediate-save behavior.'], ['Verify the effect', 'Return to the feature and test a small example. An error after saving may require a separate integration or dependency repair.']],
    questions: [['Why are some controls unavailable?', 'They may need a vault, native desktop support, or an enabled capability.'], ['Does Reset settings erase files?', 'Read the exact reset scope. Help progress reset only affects Help; other reset operations have their own contracts.']], related: ['appearance', 'shortcuts', 'google', 'advanced'],
  },
  {
    id: 'appearance', title: 'Palette, day/night, and display settings', category: 'Workspace', policy: 'manual', entry: 'Settings > Appearance; day/night control in the top bar.', prerequisite: 'No external service is required.',
    safety: 'Appearance changes display preferences, not Markdown content. High contrast is an accessibility choice, not a quality score.', source: 'src/components/AppearanceSettingsSection.tsx', roots: ['#settings-panel-appearance', '.appearance-settings'],
    steps: [['Choose the palette', 'Select the color family independently of day/night appearance. Preview the result on text, controls, and code, not only swatches.'], ['Choose Light, Dark, or System', 'Day/night changes appearance without cycling palette families. System follows the operating-system preference when supported.'], ['Tune readability', 'Adjust UI font, density, and related controls for comfort. Verify narrow windows and zoom rather than only a large desktop.'], ['Check editor synchronization', 'The editor follows application appearance until explicitly overridden. Inspect its own theme preference when it looks inconsistent.']],
    questions: [['Why should night mode not select Dracula or Nord?', 'Palette identity and light/dark appearance are independent settings.'], ['Why did the editor stay dark?', 'It may have an explicit editor-theme override. Restore synchronization in editor preferences.']], related: ['custom-theme', 'editor', 'docks'],
  },
  {
    id: 'custom-theme', title: 'Custom palette builder', category: 'Workspace', policy: 'manual', entry: 'Palette catalog > Create Custom Palette.', prerequisite: 'Choose a useful base palette and readable foreground/background values.',
    safety: 'Preview is temporary; saving persists a local custom palette. Deleting a palette should not affect note content.', source: 'src/components/themes/ThemeCustomizerModal.tsx', roots: ['.theme-customizer-modal', '.theme-customizer'],
    steps: [['Start from a base', 'Use a coherent base rather than changing unrelated colors independently. Give the palette a distinct descriptive name.'], ['Adjust and inspect', 'Check background, surfaces, text, accent, borders, and selection. A swatch alone cannot show every affected component.'], ['Respect validation', 'Resolve invalid color values and contrast errors before saving. Test keyboard focus and disabled text as well as normal text.'], ['Save or cancel', 'Save only after review; cancel restores the active palette. After deleting a custom palette, verify a valid fallback is selected.']],
    questions: [['Why can\'t I save these colors?', 'They may be invalid or fail the required contrast checks. Adjust the values rather than suppressing validation.'], ['Are custom palettes shared across all devices?', 'They use local application storage unless you explicitly transfer the relevant settings through a supported mechanism.']], related: ['appearance', 'recovery'],
  },
  {
    id: 'shortcuts', title: 'Keyboard shortcuts and conflicts', category: 'Workspace', policy: 'manual', entry: 'Settings > Shortcuts.', prerequisite: 'Know whether your platform uses Control or Command for Mod bindings.',
    safety: 'Shortcut changes alter command invocation, not native authority. Destructive commands retain their confirmation boundaries.', source: 'src/components/KeyboardShortcutsSettingsSection.tsx', roots: ['.keyboard-shortcuts-section', '#settings-panel-shortcuts', '.shortcut-table'],
    steps: [['Find an action', 'Search or browse by command rather than assigning a key to an ambiguous label.'], ['Choose a binding', 'Use the supported modifier syntax and review conflicts or invalid combinations. Avoid overriding common editing keys accidentally.'], ['Test in context', 'Some shortcuts are suppressed while typing or when a capability is unavailable. Test the actual command outside an input as appropriate.'], ['Reset narrowly', 'Restore an individual binding or the documented shortcut defaults. Verify the result after reload.']],
    questions: [['Why does F1 behave differently in the editor?', 'F1 opens contextual Help. Help does not intercept ordinary text input or submit notes.'], ['Why does a shortcut do nothing?', 'Check conflicts, focus, vault/note prerequisites, capability state, and whether the binding is applied.']], related: ['commands', 'focus', 'settings'],
  },
  {
    id: 'workspace-chrome', title: 'Customize workspace chrome and top-bar actions', category: 'Workspace', policy: 'manual', entry: 'Top-bar Customize control or Settings > Workspace.', prerequisite: 'No vault mutation or external service is required.',
    safety: 'These controls change local visibility and layout preferences only. Hiding an action does not disable its feature, cancel background work, or revoke a permission.', source: 'src/components/WorkspaceChromeSettingsSection.tsx', roots: ['.topbar-customize', '.workspace-chrome-settings', '[data-help-topic="workspace-chrome"]'],
    steps: [['Choose what stays persistent', 'Keep navigation, search, Help, and the actions you use constantly visible. Hide low-frequency launchers instead of turning the top bar into a second command palette.'], ['Restore hidden actions', 'Open Customize from the sliders control or Workspace settings. A hidden action remains available from its canonical panel, command palette, or shortcut when supported.'], ['Balance the rails', 'Sidebar and Inspector visibility are workspace chrome preferences. Collapse a rail to prioritize writing space; reopening it does not reload or recreate your notes.'], ['Recover a cramped layout', 'Restore defaults or a known layout preset when narrow windows, zoom, or many pinned actions make the shell crowded. Verify the editor remains reachable before continuing work.']],
    questions: [['Did hiding Graph disable the Graph feature?', 'No. It removes a launcher from persistent chrome. Capability enablement, plugin permission, and panel visibility are separate states.'], ['Why did some controls collapse into icons?', 'Responsive layout preserves writing space at narrow widths and high zoom. Accessible names and the command palette remain available even when visible labels are reduced.']], related: ['docks', 'commands', 'shortcuts', 'appearance'],
  },
  {
    id: 'docks', title: 'Panels, docks, resizing, and layouts', category: 'Workspace', policy: 'manual', entry: 'Panel presentation/layout controls; sidebar and inspector toggles.', prerequisite: 'Available space determines whether a companion can dock or must be modal.',
    safety: 'Layout changes are visual preferences. Hiding a panel is not cancelling its jobs, deleting its data, or revoking its permissions.', source: 'src/components/chrome/UnifiedPanelShell.tsx', roots: ['.unified-panel-docked', '.layout-presets'],
    steps: [['Choose presentation', 'Use a modal for focused work or a dock where supported for a companion view. Narrow windows may resolve docking to a modal.'], ['Preserve the editor', 'A companion may temporarily replace the inspector rather than adding another full column. Close it to recover the original rail.'], ['Resize accessibly', 'Use supported divider controls; check focus and keyboard operation. Avoid shrinking critical text below readability.'], ['Restore a layout', 'Apply a known preset or reset layout preferences when panels are lost or cramped. This does not reset document content.']],
    questions: [['Why did docking change on a small window?', 'The app reserves enough room for writing and reflows at constrained widths/zoom.'], ['Where is Help for a dock?', 'Press F1 with focus inside the dock, or open Help & guides from the single global Help entry. The same guide applies to modal and docked presentation.']], related: ['inspector', 'appearance', 'settings'],
  },
  {
    id: 'status', title: 'Status bar and background work', category: 'Workspace', policy: 'manual', entry: 'Bottom status strip and document statistics.', prerequisite: 'Read each indicator together with its label and scope.',
    safety: 'Index ready, saved, Git clean, and export success are different claims. No single green badge proves them all.', source: 'src/components/shell/WorkspaceStatusFooter.tsx', roots: ['.status-strip', '.editor-statusbar', '.status-summary'],
    steps: [['Identify the subsystem', 'Separate document save state, indexing, Git, provider synchronization, and export jobs.'], ['Wait for a real outcome', 'Queued/running state is not success. Avoid duplicate commands while a task is progressing normally.'], ['Open details', 'Use Problems, job information, or diagnostics when a status changes to error; retain the actual message.'], ['Verify recovery', 'After retry, confirm the relevant source/output, not merely that an error badge disappeared.']],
    questions: [['Does Index ready mean my last edit is saved?', 'No. Index and document persistence have separate owners and states.'], ['Why do word counts differ between note and vault?', 'They have different scopes and may update at different times.']], related: ['problems', 'activity-output', 'export-jobs', 'diagnostics', 'writing-targets'],
  },
  {
    id: 'activity-output', title: 'Activity output and operation messages', category: 'Workspace', policy: 'manual', entry: 'Output tab in the bottom status dock.', prerequisite: 'Open Output after an operation, provider action, export, save, or background task reports progress.',
    safety: 'Output is an activity log, not an authority source. A success line proves only the operation that emitted it; inspect the resulting note, artifact, account, or subsystem before relying on the outcome.', source: 'src/components/StatusDockPanel.tsx', roots: ['#dock-panel-output'],
    steps: [['Identify the emitting subsystem', 'Read the timestamp, message, and detail together so a Git, export, provider, vault, or indexing message is not attributed to the wrong feature.'], ['Follow active work', 'Running export output can update while a job is active. Wait for the responsible job/status to reach a terminal state before starting a duplicate operation.'], ['Preserve the useful error', 'When an operation fails, keep the exact error/detail long enough to diagnose the owning subsystem. Clearing or collapsing Output does not repair the cause.'], ['Verify outside the log', 'Open the resulting note, file, export artifact, integration state, or diagnostics surface to confirm the real result after a retry.']],
    questions: [['Why is Output different from Problems?', 'Output records chronological activity and operation details; Problems collects actionable diagnostics that currently need attention.'], ['Does a success line mean every related system is healthy?', 'No. It confirms the specific operation that reported success. Save state, indexing, Git, integrations, and exports have independent status.'], ['Can I clear Output safely?', 'Treat it as derived session evidence, but preserve any message you still need for troubleshooting before clearing or restarting the app.']], related: ['status', 'problems', 'export-jobs', 'diagnostics'],
  },
  {
    id: 'problems', title: 'Problems dock and actionable diagnostics', category: 'Recovery', policy: 'manual', entry: 'Problems at the bottom of the workspace.', prerequisite: 'A reported issue or a deliberate quality check.',
    safety: 'A diagnostic is evidence to inspect, not automatic authority to modify/delete content.', source: 'src/components/shell/WorkspaceStatusFooter.tsx', roots: ['.problems-panel', '.problems-dock', '.bottom-tabs-wrap'],
    steps: [['Read severity and scope', 'Identify whether the issue belongs to source syntax, indexing, export, an integration, or the runtime.'], ['Open the origin', 'Use the referenced note/path/control to inspect the actual problem rather than editing an unrelated active note.'], ['Apply the narrow repair', 'Correct the source or configuration and retry the specific operation. Preserve content before broad rewrites.'], ['Confirm resolution', 'Refresh the responsible subsystem. Dismissing a message is not proof that the cause is repaired.']],
    questions: [['Are warnings always blocking?', 'No. Read the message and affected workflow; some are advisory while others block an operation.'], ['Can I hide Problems?', 'Yes where the layout allows it, but hiding the dock does not resolve its underlying issues.']], related: ['diagnostics', 'knowledge-repair', 'recovery'],
  },
  {
    id: 'diagnostics', title: 'Diagnostics, health checks, and support evidence', category: 'Recovery', policy: 'manual', entry: 'Diagnostics, Vault health, and support/reporting controls.', prerequisite: 'Reproduce the issue and note the affected vault, feature, and version without exposing private content.',
    safety: 'Logs and screenshots can contain private data. Redact credentials, mail, document text, and unrelated paths before sharing.', source: 'src/components/DiagnosticsPanel.tsx', roots: ['.diagnostics-panel', '.health-dashboard', '.health-panel'],
    steps: [['Capture the symptom', 'Record the operation, expected result, actual error, and whether it reproduces after a safe retry.'], ['Separate scope', 'Distinguish document problems from native dependencies, network/account errors, rendering, and UI state.'], ['Collect bounded evidence', 'Use provided logs/receipts and minimal examples. Do not paste an entire vault or raw provider token.'], ['Verify a repair', 'Repeat the original user path and inspect the real result. Passing a different check is not enough.']],
    questions: [['Should I delete caches immediately?', 'First preserve useful diagnostics and confirm the cache is derived. Never treat authoritative notes as disposable cache.'], ['What makes a useful bug report?', 'A reproducible sequence, exact error, environment/version, and a redacted minimal example.']], related: ['problems', 'status', 'recovery'],
  },
  {
    id: 'hibernation', title: 'Performance HUD and subsystem hibernation', category: 'Workspace', policy: 'manual', entry: 'Performance and hibernation commands.', prerequisite: 'Understand which subsystem you are pausing.',
    safety: 'Pausing watchers, Git refresh, graph, or proofing can intentionally make derived information stale. Resume before relying on it.', source: 'src/lib/buildPaletteCommands.ts', roots: ['.perf-hud', '.performance-hud', '.graph-hibernated-placeholder'],
    steps: [['Measure a real symptom', 'Compare interaction latency and resource use under a repeatable workload rather than treating one number as a quality grade.'], ['Pause narrowly', 'Hibernate only the relevant subsystem and read its paused indicator.'], ['Continue with the limitation', 'Expect suspended background refresh or computation. Do not mistake a frozen view for current source state.'], ['Resume and recheck', 'Wake the subsystem and allow it to catch up before exporting, synchronizing, or trusting derived information.']],
    questions: [['Does hibernation delete data?', 'It pauses work; it is not intended as deletion or undo.'], ['Why did links stop updating?', 'Check watcher/index-related status and hibernation before rebuilding or editing files unnecessarily.']], related: ['status', 'graph', 'diagnostics'],
  },
  {
    id: 'history', title: 'Note history and revision recovery', category: 'Recovery', policy: 'manual', entry: 'Note history timeline for the active note.', prerequisite: 'An active note with recorded revisions.',
    safety: 'Restoring a revision replaces current content through a deliberate workflow. Preserve current edits before accepting a restore.', source: 'src/components/NoteHistoryPanel.tsx', roots: ['.note-history-panel', '.note-history'],
    steps: [['Confirm the note', 'Check the path and revision timestamp. History for one note is not the complete vault backup history.'], ['Compare revisions', 'Inspect the contents/diff and select the revision that actually contains the missing work.'], ['Restore deliberately', 'Review what will be replaced and preserve any newer useful text separately.'], ['Verify on disk', 'Confirm the restored content and save status; review links/metadata after a major rollback.']],
    questions: [['Why is no history available?', 'The feature can only show retained revisions it has recorded. Use Git/backups when the desired version was not captured.'], ['Is history a disaster-recovery backup?', 'No. Local history can be lost with the same device or vault. Keep verified external backups.']], related: ['backups', 'restore', 'git', 'recovery'],
  },
  {
    id: 'recovery', title: 'Save errors, stale state, and safe recovery', category: 'Recovery', policy: 'manual', entry: 'Error/status surfaces, history, conflicts, and backups.', prerequisite: 'Identify the authoritative source and preserve useful unsaved work.',
    safety: 'Recovery steps can discard or replace content. Do not reset, reload, or restore until the consequence is understood.', source: 'src/hooks/useWorkspaceEditor.ts', roots: ['.panel-error-fallback', '.error-boundary'],
    steps: [['Preserve evidence and text', 'Record the exact error and keep a safe copy of useful unsaved content before destructive recovery.'], ['Identify the failing owner', 'A preview error, stale index, save failure, conflict, and missing external tool require different repairs.'], ['Choose the narrow action', 'Retry a failed read, fix configuration, resolve a conflict, or restore a verified revision as appropriate.'], ['Verify the original path', 'Reopen the note/output and repeat the operation. A dismissed error is not proof of recovered data.']],
    questions: [['Should I reload immediately?', 'Not while useful unsaved work might be lost. Read the available recovery choices and preserve it first.'], ['Can a rebuilt index recover deleted prose?', 'No. The index is derived. Use source history, Git, or a verified backup.']], related: ['history', 'conflicts', 'backups', 'diagnostics'],
  },
  {
    id: 'backups', title: 'Local snapshots and external backups', category: 'Recovery', policy: 'manual', entry: 'Backup controls in settings/advanced workflows.', prerequisite: 'Open the intended vault and select an appropriate storage target.',
    safety: 'Local snapshots and external disaster-recovery backups have different failure domains. Verify manifests and test restoration.', source: 'src/hooks/useVaultBackup.ts', roots: ['.backup-settings', '.backup-panel'],
    steps: [['Choose the target', 'Local snapshots are quick recovery inside the vault; an independent external target protects against loss of the original location.'], ['Review scope', 'Check what files are included, destination binding, available disk space, and whether sensitive material is copied.'], ['Create and verify', 'Wait for completion and inspect the manifest/result. A schedule enabled in settings is not proof that a recent backup succeeded.'], ['Practice restoration', 'Use a safe, controlled drill and preserve the original. Confirm document content, attachments, and needed sidecars.']],
    questions: [['Is a snapshot beside my notes enough?', 'Not for device or vault loss. Use an independent verified backup for disaster recovery.'], ['Why did an automatic backup request approval?', 'Native backup writes have explicit authority boundaries; inspect the requested scope.']], related: ['restore', 'history', 'recovery'],
  },
  {
    id: 'restore', title: 'Restore a verified backup', category: 'Recovery', policy: 'manual', entry: 'Restore in the backup workflow.', prerequisite: 'A verified backup belonging to the intended vault and a safe plan for current content.',
    safety: 'Restoration can replace current files. It is deliberately manual and never part of an automatic first-use tour.', source: 'apps/desktop/src-tauri/src/commands/backup.rs', roots: ['.restore-dialog', '.backup-restore-panel'],
    steps: [['Preserve current work', 'Make a separate safe copy before restoration, especially if current files contain useful changes.'], ['Verify the backup', 'Review vault binding, paths, hashes, sizes, and timestamp. Do not use an unrelated or damaged snapshot.'], ['Confirm the exact restore', 'Read scope and consequence in the native confirmation. Cancel when the target is not what you intended.'], ['Validate recovery', 'Inspect restored documents/attachments and the restore result. Investigate partial failure or journal evidence before continuing ordinary edits.']],
    questions: [['Can Help demonstrate Restore on my files?', 'No. Tours never execute recovery or destructive operations.'], ['Why is a backup rejected?', 'Manifest integrity, path safety, vault binding, or missing data may fail verification. Preserve it and diagnose rather than bypassing checks.']], related: ['backups', 'recovery', 'diagnostics'],
  },
  {
    id: 'git', title: 'Git status, diffs, and selected commits', category: 'Recovery', policy: 'manual', entry: 'Git panel from the command palette or configured top bar.', prerequisite: 'A supported Git repository and configured identity where required.',
    safety: 'Commit, pull, push, discard, and conflict actions are different mutations. Review exactly which files are selected.', source: 'src/components/GitPanel.tsx', roots: ['.git-panel'],
    steps: [['Read repository state', 'Distinguish loading, non-repository, clean, changed, and error. A failed status read is not a clean repository.'], ['Inspect diffs', 'Review source changes and untracked paths; exclude private or generated content that should not be committed.'], ['Commit selected changes', 'Choose intended files and a useful message. Verify the result rather than assuming a button press created a commit.'], ['Handle remotes separately', 'Push/pull require their own authorization, network, and conflict handling. A local commit is not automatically uploaded.']],
    questions: [['Will Scriptor commit every staged change?', 'Use the explicit selected-files contract and inspect the commit. Do not infer scope from a generic button label.'], ['Is Git a complete backup?', 'It only protects committed/tracked content available in the relevant repository copies. Keep independent backups for everything important.']], related: ['conflicts', 'history', 'backups'],
  },
  {
    id: 'conflicts', title: 'Conflicts and changed-on-disk recovery', category: 'Recovery', policy: 'manual', entry: 'Conflict resolver or save-integrity warning.', prerequisite: 'Understand the local, disk, and proposed versions before resolving.',
    safety: 'Choosing a version can discard another. Never auto-accept a side merely to unblock saving.', source: 'src/components/ConflictResolverModal.tsx', roots: ['.conflict-resolver', '.conflict-resolver-modal'],
    steps: [['Identify versions', 'Read which text is local, on disk, or from another revision. Confirm the note path.'], ['Compare intentionally', 'Preserve useful changes from both sides and inspect every conflict section.'], ['Review merged output', 'Check the complete proposed Markdown, including frontmatter and references, before applying.'], ['Verify persistence', 'Confirm the saved result and resulting Git/index state. Keep diagnostic evidence if applying fails.']],
    questions: [['Does Keep editing overwrite the disk version immediately?', 'Read the particular warning/action contract; continuing editing and accepting a disk overwrite are not interchangeable.'], ['Can I close the dialog and resolve later?', 'Preserve unsaved text and understand what the close/cancel action retains before doing so.']], related: ['recovery', 'git', 'history'],
  },
  {
    id: 'rename', title: 'Rename notes and rewrite links', category: 'Recovery', policy: 'manual', entry: 'Rename active note and link-rewrite dialogs.', prerequisite: 'The correct active note and a valid destination path.',
    safety: 'Renaming/rewrite can affect several files. Preview scope and preserve a recovery path.', source: 'src/components/RenameNoteDialog.tsx', roots: ['.rename-dialog', '.link-rewrite-dialog'],
    steps: [['Choose the destination', 'Use a valid vault-relative path and check for collisions. Similar filenames may be distinct notes.'], ['Preview affected links', 'Read the dry-run result and expected rewritten references. External/unmanaged links may need separate attention.'], ['Apply deliberately', 'Confirm only the intended rename/rewrite. Resolve stale source or destination errors by refreshing the plan.'], ['Verify navigation', 'Open the new path, check backlinks/outgoing links, and inspect Git/history for unexpected changes.']],
    questions: [['Why not rename directly in the file manager?', 'External renames may not perform the same reviewed link updates. Inspect references afterward.'], ['Can the guide perform the rename?', 'No. It explains the workflow without changing paths or documents.']], related: ['backlinks', 'outgoing', 'git'],
  },
  {
    id: 'import', title: 'Import an Obsidian vault', category: 'Writing', policy: 'manual', entry: 'Import Obsidian vault from the command palette or vault menu.', prerequisite: 'A readable source vault, safe destination, and enough storage.',
    safety: 'Import writes local files. Back up source/destination first and inspect collision/conversion behavior.', source: 'src/components/ObsidianImportDialog.tsx', roots: ['.obsidian-import-dialog'],
    steps: [['Select the source', 'Confirm the original folder and preserve it. Do not point the importer at an unintended parent directory.'], ['Review options', 'Inspect destination, attachments, supported conversions, and collision handling. Unsupported app-specific behavior may not transfer.'], ['Run and read the result', 'Wait for the bounded operation and inspect counts/errors rather than assuming every source file imported.'], ['Validate examples', 'Open representative notes, links, attachments, frontmatter, and diagrams before relying on the migrated vault.']],
    questions: [['Will every Obsidian plugin feature work?', 'No. Markdown portability does not guarantee compatibility with every plugin-specific syntax or behavior.'], ['Can I delete the original immediately?', 'Keep it until you have verified the imported content and an independent backup.']], related: ['vault', 'preview', 'backups'],
  },
  {
    id: 'advanced', title: 'Advanced settings and operational tools', category: 'Integrations', policy: 'manual', entry: 'Settings > Advanced and operational command surfaces.', prerequisite: 'Know the intended subsystem, capability maturity, and dependency requirements.',
    safety: 'Advanced does not mean unrestricted. Process, network, keychain, backup, and native writes retain their explicit boundaries.', source: 'src/components/AdvancedSettingsSection.tsx', roots: ['#settings-panel-advanced', '.advanced-settings'],
    steps: [['Identify the owner', 'Choose the setting for the actual failing subsystem rather than toggling unrelated experimental features.'], ['Check dependencies', 'Review executables, endpoints, resources, and bounded limits. A missing tool should produce an actionable error.'], ['Change one variable', 'Record the original setting and test with safe data. This makes regressions easier to isolate.'], ['Return to a known state', 'Disable unneeded experiments and confirm normal editing/search still work. Preserve logs without sharing secrets.']],
    questions: [['Are encryption/mobile/public marketplace options fully shipped?', 'Consult the capability maturity ledger. Design-only or incubating modules are not guarantees of available product workflows.'], ['Does a tool setting grant all future access?', 'No. Native operations validate scope and authorization independently.']], related: ['permissions', 'code-chunks', 'diagnostics'],
  },
  {
    id: 'help', title: 'Use Help, questions, and replayable tours', category: 'Workspace', policy: 'manual', entry: 'F1 for contextual guidance, or the single Help & guides entry in the top bar or command palette.', prerequisite: 'Help works offline and does not require a vault.',
    safety: 'This is curated product guidance, not an AI chat or an automatic operator. Search text is not sent to a service.', source: 'src/components/help/HelpRuntime.tsx', roots: ['.help-center'],
    steps: [['Open in context', 'Press F1 while focus is inside the feature you need help with. The global Help entry opens the general Help browser without adding controls to every panel.'], ['Search the knowledge base', 'Search guide titles, steps, or answered questions. Inspect the result category and prerequisites before following instructions.'], ['Replay or resume', 'Start a tour, navigate steps, or resume stored progress. Show this control reveals an existing control without clicking it; F1 returns to the tour.'], ['Reset deliberately', 'Reset guide progress only after confirming; it does not touch notes, vault content, permissions, or account credentials.']],
    questions: [['Why do detailed guides appear in English?', 'The current authored guide corpus is English. Help controls support English, German, and Persian; a visible notice identifies the English content rather than silently pretending it is translated.'], ['What if a control is missing?', 'The feature may be closed, disabled, unavailable, or narrower than expected. Follow its entry/prerequisite instructions; Help will not auto-enable or navigate a dangerous workflow.']], related: ['workspace', 'commands', 'shortcuts'],
  },
]
