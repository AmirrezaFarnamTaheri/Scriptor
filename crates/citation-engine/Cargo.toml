[package]
name = "scriptor-citation-engine"
version = "0.1.0"
edition = "2024"
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
hayagriva = "0.5"
serde = { version = "1", features = ["derive"] }
thiserror = "2"
