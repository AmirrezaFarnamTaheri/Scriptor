[package]
name = "scriptor-citation-engine"
version = "0.0.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
hayagriva = "0.5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
