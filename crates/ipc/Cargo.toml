[package]
name = "scriptor-ipc"
version = "0.0.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
postcard = { version = "1", features = ["alloc"] }
serde = { version = "1", features = ["derive"] }
thiserror = "2"
