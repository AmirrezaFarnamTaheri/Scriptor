# `scriptor-native-git`

Safe Git integration for vault roots using explicit `git` argument vectors (no shell).

## Shipped

- Status, commit selected paths, pull, push.
- Merge conflict detection and marker preview.
- Conflict resolution helper for note paths.

Tauri commands: `git_*`. Headless parity: daemon `GitStatus` RPC.
