[package]
name = "scriptor-wasm-runtime"
version = "0.0.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
default = []
wasmtime-backend = []
