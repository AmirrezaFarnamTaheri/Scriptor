[package]
name = "scriptor-tantivy-indexer"
version = "0.0.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
tantivy = "0.22"
thiserror = "2"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
