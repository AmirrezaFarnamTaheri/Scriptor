[package]
name = "scriptor-wasm-runtime"
version = "1.0.3"
edition = "2024"
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
thiserror = "2"
serde = { version = "1", features = ["derive"] }

[features]
default = []
wasmtime-backend = []
